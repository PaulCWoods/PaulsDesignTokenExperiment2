import { create } from '@storybook/theming';

export default create({
  base: 'light',
  brandTitle: `<img src="Logo.svg" alt="Frosted Tokens" style=max-width="250px" />`,
  brandUrl: 'https://storybook.designtokenscourse.com/',
  brandTarget: '_self'
});
